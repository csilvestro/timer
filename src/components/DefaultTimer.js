const DefaultTimer = () => {
    return(
        <p style={styles.container}>Default Timer: </p>
    )
}
export default DefaultTimer;

const styles = {
    container: {
        display: 'flex',
        flexDirection: 'row'
    }
};